# 本地自动排版版

正式入口：https://pangpanglovepigpig.github.io/wenzhangyijianpaiban/

文章排版、手工编辑、分页和图片生成全部在浏览器内完成。网站不再调用 AI 接口，不需要 DeepSeek 密钥，也不依赖 Vercel 后端。原有 Vercel 项目若继续自动部署，只提供同样的静态页面。

## 运行与发布

使用 Node.js 20.19 或更新版本（CI 使用 Node.js 22）：

```bash
npm ci
npm test
npm run build
npm run dev
```

GitHub Pages 由 `.github/workflows/deploy-pages.yml` 在 main 更新时发布，构建只设置 `VITE_BASE_PATH=/wenzhangyijianpaiban/`。部署到根路径时使用 `/`。实际入口为 `src/main.tsx`，根目录早期同名文件不是当前构建入口。

不再设置 `VITE_ENABLE_AI_DRAFT`、`VITE_AI_DRAFT_ENDPOINT` 或任何 DeepSeek 环境变量；即使旧项目控制台仍保存这些变量，新代码也不会读取它们。原 AI 接口和浏览器请求模块已删除。未操作第三方账号中的密钥。

## 本地规则

- 明确的 `#`、`##`、`###` 保留级别；短且独立的普通首行可作为标题，长正文不强制升级。
- 清单保留原项目符号和编号；小标题必须有结构依据及后续正文，自动分区需要足够前文和转折信号。
- 正文以自然段和完整句为单位，通常每块 50–100 字。只有长单句才在引号、括号之外寻找分句位置；没有安全位置则保留长段，由图片分页处理。
- 自动重点仅使用局部红/蓝文字，不叠加加粗、高亮和下划线；每分区至多两处、全文至多六处，总字数不超过正文 20%。手工高亮等工具仍可使用。
- 原文校验独立于标题识别：保留字符、标点、数字及列表符号，忽略布局空白和明确的 Markdown 标题/分隔线标记。失败时退回原段落并提示。
- 长段按实际画布测量跨页，仅拆分图片中的显示片段，不修改编辑区文字。中文标点避免落在行首，组合 emoji 与附加符号不拆开。

## 缓存与离线

当前缓存版本为 `xiaohongshu-article-notes-v13`。服务工作线程安装时缓存当前页面、带版本的脚本和样式；缓存成功后才接管页面。更新程序会清理旧缓存。

首次打开仍需要网络，且须等待资源缓存完成；之后可在断网时重新打开页面、排版、生成图片和下载。浏览器清理网站数据或禁用缓存后，需要再次联网打开。用户输入和手工编辑目前不跨页面刷新保存。

## 验收

```bash
npm test
npm run build
```

可选浏览器验收使用 Playwright Chromium（先自行准备 Playwright 及对应浏览器）：

```bash
LAYOUT_PLAYWRIGHT_PATH=/absolute/path/to/playwright node scripts/verify-layout-ui.mjs
```

若本地已安装可解析的 `playwright` 包，可省略路径。脚本启动生产预览，检查真实页面交互、十二种主题和三档字号、长段完整性、手机尺寸、四倍 CPU 降速、预览失败重试、离线刷新与 PNG 下载，以及没有 AI 请求。

可选 `LAYOUT_QA_URL` 指向已部署网址；`LAYOUT_QA_ARTICLES` 指向本地 JSON 数组（每项包含 `title` 与 `text`）；`LAYOUT_QA_OUTPUT` 指定报告和截图位置。真实 WPS 文章仅作为本地验收输入，不提交仓库。手机尺寸和 CPU 降速属于模拟测试，不等于实机测试。
