DEEPSEEK_API_KEY=
# 默认使用 deepseek-v4-flash，生成更快；如需更高质量可改为 deepseek-v4-pro。
# DEEPSEEK_MODEL=deepseek-v4-flash
# DeepSeek 等待时间，默认 55000 毫秒，最高建议不超过 55000。
# DEEPSEEK_TIMEOUT_MS=55000
# 默认关闭本地规则兜底，避免 DeepSeek 失败时生成低质量初稿。
# 如需临时应急，可改为 true。
# DEEPSEEK_ENABLE_LOCAL_FALLBACK=false
